name: Bicep CI/CD

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  bicep-deploy:
    runs-on: ubuntu-latest
    env:
      BICEP_FILE: iac-bicep/envs/dev/main.bicep
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Azure Login
        uses: azure/login@v1
        with:
          creds: |
            {
              "clientId": "${{ secrets.ARM_CLIENT_ID }}",
              "clientSecret": "${{ secrets.ARM_CLIENT_SECRET }}",
              "subscriptionId": "${{ secrets.ARM_SUBSCRIPTION_ID }}",
              "tenantId": "${{ secrets.ARM-TENANT_ID }}"
            }

      - name: Bicep Lint
        run: az bicep build --file $BICEP_FILE

      - name: Bicep What-If (Preview)
        run: az deployment group what-if --resource-group iac-modular-rg --template-file $BICEP_FILE

      - name: Bicep Deploy (manual approval)
        if: github.event_name == 'workflow_dispatch'
        run: az deployment group create --resource-group iac-modular-rg --template-file $BICEP_FILE
